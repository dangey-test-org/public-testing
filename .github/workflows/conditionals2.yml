on:
  workflow_dispatch:

jobs:
  pre:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'done'

  task:
    needs: pre
    if: ${{ failure() }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'done'
